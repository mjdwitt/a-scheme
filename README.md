a simple scheme in haskell

Requires ghc and cabal. Build with `cabal build` and run the resulting
`dist/build/scheme/scheme`. Or install with `cabal install`. Best to develop
using newfangled cabal sandboxes.
